{
  "name": "nfr",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e": "vitest run src/happy.spec.ts",
    "e2e:ui": "E2E_HEADED=1 E2E_KEEP_OPEN=1 vitest --ui --watch --open src/happy.spec.ts",
    "test:load": "for f in load-scenarios/*.yml; do artillery run --config artillery.yml \"$f\" || exit 1; done"
  },
  "devDependencies": {
    "@vitest/browser-playwright": "^5.0.1",
    "@vitest/ui": "5.0.1",
    "artillery": "^2.0.34",
    "playwright": "^1.62.1",
    "vitest": "^5.0.1"
  }
}
