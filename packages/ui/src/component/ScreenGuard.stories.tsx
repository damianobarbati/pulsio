import { ScreenGuard } from './ScreenGuard.tsx';

export default { title: 'UI/ScreenGuard', component: ScreenGuard };

export const Responsive = { render: () => <ScreenGuard>Resize the viewport to see the screen guard.</ScreenGuard> };
